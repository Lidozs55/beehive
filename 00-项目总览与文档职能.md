# 《蜂巢》项目文档结构指引

> 本文件是蜂巢小说项目的文档入口和协作协议。它说明“信息应该放在哪里、写作前读什么、写作后更新什么”，不重复世界观和正文内容。
>
> 读取原则：先读本文件，再按当前任务加载最小必要文件。项目规模数字是预算或结构镜像，不是文学质量保证；状态文件只记录已经发生或当前确实需要交接的事实。

## 1. 项目边界

- `正文/` 是交付正文的唯一目录。文件名使用 `P章号-标题.md`，如 `P070-完整还原.md`；新章沿用已有编号，不用临时标题覆盖旧章。
- `bible/` 记录世界边界、制度、技术、社会、场次和历史。它回答“这个世界允许什么、已经规定什么”。
- `characters/` 记录人物的动机、行为边界、能力、关系和当前可用信息。它不替代正文中的人物变化。
- `chapters/` 记录全书、卷、叙事单元、发布章、开篇细卡和结构检查。只有 `P001-P018` 预先制作了逐章明细场景卡；`P019` 起的发布章表是事件骨架，不应被当成完整场景卡。后续章节由当前大纲、状态和主代理在 `state/handoff.md` 中形成临近写作简报。
- `arcs/` 记录故事弧及弧内交接材料。若某条线已在这里有唯一场景卡，不要在其他目录复制一份平行版本。
- `state/` 记录实际写作进度、已写事实、章节摘要、时间线、人物当前状态、开放钩子和机器检查所需镜像。它回答“已经写到哪里、现在谁知道什么、下一步是什么”。
- `meta/` 记录项目裁决、历史材料兼容说明、方法参考和主代理工作协议。它回答“为什么这样定、冲突如何裁决、如何派发”。
- `audits/` 保存已经发生的审查记录和回归证据。它记录问题和修复，不作为新的剧情正典。
- `tools/` 保存只读采集、结构检查和算术校验脚本。脚本结果需要人工解释，不能替代文学判断。
- 根目录 `style-guide.md` 是正文风格和表达纪律的唯一项目级风格入口。

## 2. 权威关系

出现冲突时按以下顺序处理：

1. 作者或用户本次明确裁决。
2. 已写正文及 `state/established-facts.md` 的正文事实追加区。
3. 相关人物档案、世界观硬约束和已确认的重大裁决。
4. 当前章节卡、故事弧和大纲中的未来计划。
5. 摘要、机器镜像、审查意见和一般创作经验。

这里的“优先”不是静默覆盖。若高层文件与已写正文冲突，停止扩写冲突段，在 `meta/04-重大裁决与审阅顺序.md` 或相应审查记录中登记裁决，再同步受影响文件。

区分三类内容：

- **已写事实**：正文中已经发生，后续默认不可悄悄改写。
- **候选正典/计划**：大纲、bible、章节卡中预计发生或已冻结但尚未在正文兑现的内容。
- **推论/审查意见**：角色或审稿人根据材料得出的判断，除非被正文或裁决确认，不得直接写成事实。

## 3. 文档职责与推荐入口

| 任务 | 首要入口 | 按需补充 |
| --- | --- | --- |
| 了解项目方向 | `meta/04-重大裁决与审阅顺序.md` | `chapters/00-总大纲.md`、相关 bible |
| 写新章节 | `state/handoff.md` | P001-P018加读明细场景卡；之后读取事件骨架、上一章正文、人物档案、事实与时间线区间 |
| 续写长线 | `state/handoff.md`、`state/character-state.md` | `state/next-hook.md`、`state/foreshadows.json`、当前弧线 |
| 核对事实 | `state/established-facts.md` | `bible/`、`characters/`、`state/timeline-log.md` |
| 规划卷/弧 | `chapters/00-总大纲.md` | `chapters/01-04`、`arcs/`、`state/foreshadows.json` |
| 设计试炼或多世界机制 | `bible/试炼世界/00-试炼总则.md` | `novel-skill` 的 `12-多世界与试炼结构.md`、相关场次文件 |
| 风格校对 | `style-guide.md` | 最近连续正文、`novel-skill` 的 `07-文风与语言修订.md` |
| 审查或改稿 | `meta/04-重大裁决与审阅顺序.md` | `chapters/08-规则反例与解法检查.md`、相关 audits |
| 派发子代理 | `meta/派发模板.md` | 运行 `python tools/gather_dispatch.py P0XX` 生成参数块 |

## 4. 写作前的最小读取路径

### 主代理

1. 本文件和 `style-guide.md`。
2. `state/handoff.md`，确认当前位置、下一章目标、知情边界和当前写作注意。
3. 本章的章节卡、故事弧条目，或 `state/handoff.md` 中足以执行的章节简报。简报至少覆盖目标、场景/事件链、证据与权限来源、知情边界、时间地点和结束变化；信息不足时先补简报，不为形式重复建卡。
4. `state/character-state.md` 中出场人物的当前状态。
5. `state/established-facts.md` 的本章及邻近事实；需要时查相关 `bible/` 和人物档案。
6. 运行 `python tools/slice_summaries.py 上一章 本章` 提取两章节点；不要全量读入 `state/summaries.md`。连续性审查才按明确 PID 范围提取。
7. `state/timeline-log.md` 的本章时间地点行。
8. 上一章正文全文；涉及证据链、原话、物件或方法时向前回读来源章节。

### 写作子代理

子代理只读取主代理派发中明确列出的文件和区间，不自行通读整个项目，不根据目录猜测未派发信息。派发材料不足以保证知情边界、事实连续性或本章目标时，先在回执中报告缺口，不擅自补设定。

## 5. 写作后的状态流转

子代理：

- 只创建或覆盖主代理指定的 `正文/Pxxx-标题.md`。
- 不修改 `state/`、`bible/`、`characters/`、`chapters/`、`meta/` 或 `audits/`，不运行 git 写操作。
- 回执报告正文路径、要点覆盖、关键事实、未解决问题和建议状态变更；回执不是状态文件本身。

主代理：

1. 阅读子代理正文并按当前风险做事实、视角、风格和结构复核。
2. 更新 `state/established-facts.md` 的正文事实追加区，记录事实 ID、P 章、原文定位和后续约束。
3. 更新 `state/summaries.md`、`state/timeline-log.md`、`state/character-state.md`、`state/next-hook.md` 和 `state/handoff.md` 中实际变化的部分。
4. 若发生大纲、规则或人物裁决，先更新 `meta/04-重大裁决与审阅顺序.md`，再同步受影响文件。
5. 运行 `python tools/validate_workspace.py`；涉及人口、资源或结构镜像时再运行对应工具。脚本失败要区分本次变更、旧问题和脚本假设，不用“检查通过”代替人工审稿。

没有变化的文件不做形式化刷新。状态台账的目的是真实交接，不是每章制造文档噪声。

## 6. 文件命名与版本规则

- 章节正文：`正文/P###-短标题.md`。
- 章节结构：`chapters/` 下按已有编号和职责命名；同一章节不要同时维护两个未注明主次的卡片。
- 状态文件：沿用现有英文稳定文件名和既有字段，先更新内容，不随意改名。JSON 是机器镜像，字段变更必须同步工具或先保留兼容字段。
- 审查文件：`audits/YYYY-MM-DD-主题.md`；同一轮复查用 `R1/R2/...` 区分。审查报告提出的建议只有被裁决或写入正文后才升级为正典。
- 临时材料、子代理草稿、扫描输出和日志不得留在项目目录；需要长期保留时，归入明确职责的目录并写清来源。

## 7. 当前重要入口

- 项目裁决：`meta/04-重大裁决与审阅顺序.md`
- 当前交接：`state/handoff.md`
- 已写事实：`state/established-facts.md`
- 当前人物：`state/character-state.md`
- 章节摘要：`state/summaries.md`
- 故事时间线：`state/timeline-log.md`
- 总体结构：`chapters/00-总大纲.md`
- 开篇场景卡：`chapters/06-开篇18章写作卡.md`
- 规则反例：`chapters/08-规则反例与解法检查.md`
- 机器校验：`tools/validate_workspace.py`

数字预算、章节数和状态镜像服务于协作与检查，不表示正文已完成，也不要求所有远期设定在当前写作前全部填满。
